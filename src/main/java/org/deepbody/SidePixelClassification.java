package org.deepbody;

/**
 * Created by john on 14.12.16.
 * Classify the image into parts: head (A), tors (B), arms (C), legs (D), and others (E)
 * If the classification between tors and arms is not accurate,
 * we can merge them as one class and distinguish the arm edges
 */
public class SidePixelClassification {
}
